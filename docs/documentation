Main.java - main файл программы
содержит осноывные переменные:
winWidth, winHeight - размеры окна
cols, rows - кол-во столбцов и строк в таблице
cellSize - размер клетки
создает окно (frame)



GridFrame.java - отрисовка таблицы, настройки окна
в конструкторе создаётся таблица (grid)



Grid.java - класс таблицы, описание логики таблицы
таблица хранится в двумерном массиве целых чисел
содержит различные методы заполнения таблицы:
initializeGrid() - заполнение нулями (полностью белым)
edgeCheckGrid() - заполнение края таблицы 2 (красным), чтобы отследить что вся таблица помещается в окно
randomGrid() - рандомное заполнение таблицы (шум)

paint() - метод отрисовки таблицы, рисуем границы клеток - серые прямоугольники, и заполняем их соответсвующим цветом